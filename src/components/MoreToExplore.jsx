import React from "react";

const MoreToExplore = ({styles}) => {
    return (
        <span className={`opacity-50 text-black font-bold text-[11px] tracking-[0.15em] ${styles}`}>

        </span>
    )
}

export default MoreToExplore;